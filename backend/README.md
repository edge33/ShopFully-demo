# backend

This is the backend part for the flyers page demo

In order to run:

```
$: npm install
$: npm run dev
```

In order to build:

```
$: npm run build
$: npm start
```

copy src/input in dist/ folder in order to provide data to expose with the api